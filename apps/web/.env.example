# Token de demonstração pública pré-anexado pelo front nas rotas /admin
# (simular/reenviar). NÃO é segredo por design: vive no bundle publicado e a
# defesa real de abuso é o rate-limit agressivo do servidor. Precisa bater com
# o DEMO_TOKEN da API.
VITE_DEMO_TOKEN=demo-nao-secreto
